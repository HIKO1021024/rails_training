<%= form_for(info) do |f| %>
  <% if info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(info.errors.count, "error") %> prohibited this info from being saved:</h2>

      <ul>
      <% info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
　<div class="field_info_title">
　  題名
　</div>  
  <div class="field">
    <%= f.text_field :title %>
  </div>
　
　<div class="field_info_info">
　  内容
　</div>
　
  <div class="field">
    <%= f.text_area :info %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
